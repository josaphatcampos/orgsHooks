import producers from '../mocks/producers.ts';
import headerData from '../mocks/header.ts';

export const requestProducers = () => {
  return producers;
};

export const requestHeader = () => {
  return headerData;
};
